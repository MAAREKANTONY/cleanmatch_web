default_provider: existing_or_nominatim
providers:
  existing_only:
    enabled: true
  existing_or_nominatim:
    enabled: true
    user_agent: cleanmatch-web
    timeout_seconds: 10
    min_delay_seconds: 1
    source_label: nominatim
